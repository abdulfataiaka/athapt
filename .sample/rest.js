const RestParser = require('@athapt/rest');

const parser = new RestParser();

// Parser are for converting requests into objects with exact attributes we will need
// Can extend existing parser based on a defined specification
// Create a parser from scratch based on prefered specification
